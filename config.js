// Optional backend URL.
// Leave empty to use direct Internet Archive downloads from GitHub Pages.
// After deploying the Cloudflare Worker in /worker, paste its URL here.
window.RANDOM_FILE_CONFIG = {
  workerBaseUrl: ""
};
